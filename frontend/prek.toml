[[repos]]
repo = "local"
hooks = [
    { id = "biome-format", name = "biome format", entry = "bun run format", language = "system", pass_filenames = false },
    { id = "biome-lint", name = "biome lint", entry = "bun run lint", language = "system", pass_filenames = false },
]
